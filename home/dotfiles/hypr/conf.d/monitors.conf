# Auto-detect the virtual display and follow the VM window size.
monitor = , preferred, auto, 1

# If auto-resize misbehaves, pin a fixed resolution instead, e.g.:
# monitor = Virtual-1, 1920x1080@60, 0x0, 1
