# Fixed resolution to match the MacBook's logical resolution.
# For this to stick, turn OFF "Resize display to window size automatically"
# in UTM → Display, otherwise UTM keeps overriding it with the window size.
monitor = , 1800x1169@60, auto, 1

# To go back to auto-resizing with the VM window:
# monitor = , preferred, auto, 1
